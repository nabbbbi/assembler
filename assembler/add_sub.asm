	add eax, ecx		; add low orders of value
	adc edx, ebx		; and high orders with counting carry. perenos.

	sub eax, ecx 		; substract low orders
	sbb edx, ebx		; and high orders with counging loan. zaim
